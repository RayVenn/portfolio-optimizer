Crypto Monitor has been deployed.

Release: {{ .Release.Name }}
Image:   {{ .Values.image.repository }}:{{ .Values.image.tag }}

To check producer health:
  kubectl port-forward svc/{{ include "crypto-monitor.fullname" . }} 8080:{{ .Values.service.port }} -n {{ .Release.Namespace }}

Then:
  curl http://localhost:8080/health
