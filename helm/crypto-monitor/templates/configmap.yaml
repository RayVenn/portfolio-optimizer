apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "crypto-monitor.fullname" . }}-config
  labels:
    {{- include "crypto-monitor.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
